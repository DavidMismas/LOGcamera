# Rawlight project notes

- Review `ROADMAP.md` before proposing or implementing the next product feature.
- Do not change `CURRENT_PROJECT_VERSION` or the App Store build number. The user manages build numbers.
- Do not install the app on a physical iPhone. The user handles device installation and testing.
- Detect camera features from AVFoundation capabilities instead of hard-coding iPhone model names.
